---
import BaseLayout from "../../../layouts/BaseLayout.astro";
---
<BaseLayout title="Introducción a GCP - Cloud Fundamentals">
  <div class="gcp-container">
    <!-- Navigation Back -->
    <div class="navigation-back">
      <a href="/devsecops/google-devops-sre/cloud-fundamentals">← Volver a Cloud Fundamentals</a>
    </div>

    <!-- Hero Section -->
    <div class="gcp-hero">
      <h1 class="gcp-title">Introducción a Google Cloud Platform</h1>
      <p class="gcp-subtitle">Fundamentos de infraestructura, seguridad y gestión de costes en GCP</p>
    </div>

    <!-- Main Content -->
    <div class="content-section">

      <!-- Section 1: IaaS, PaaS y SaaS -->
      <section class="topic-section" id="iaas-paas-saas">
        <h2 class="section-title">1. IaaS, PaaS y SaaS</h2>
        
        <div class="content-card">
          <h3 class="card-title">IaaS (Infraestructura como Servicio)</h3>
          <ul class="info-list">
            <li><span class="label">¿Qué es?</span> Proporciona acceso a recursos puros de computación, almacenamiento y red.</li>
            <li><span class="label">Modelo de pago:</span> Los clientes pagan por los recursos que <strong>asignan por adelantado</strong>, independientemente de su uso.</li>
            <li><span class="label">Ejemplo:</span> Máquinas virtuales en Compute Engine.</li>
          </ul>
        </div>

        <div class="content-card">
          <h3 class="card-title">PaaS (Plataforma como Servicio)</h3>
          <ul class="info-list">
            <li><span class="label">¿Qué es?</span> Vincula el código a librerías que dan acceso a la infraestructura que la aplicación necesita. Permite centrarse más en la lógica de la aplicación.</li>
            <li><span class="label">Modelo de pago:</span> Los clientes pagan por los recursos que <strong>realmente utilizan</strong>.</li>
            <li><span class="label">Ejemplo:</span> App Engine.</li>
          </ul>
        </div>

        <div class="content-card comparison-card">
          <h3 class="card-title">Diferencia entre PaaS y Serverless</h3>
          <p class="card-intro">Aunque relacionados, representan distintos niveles de abstracción:</p>
          
          <div class="comparison-grid">
            <div class="comparison-item">
              <h4>PaaS (Plataforma como Servicio)</h4>
              <ul class="info-list">
                <li><span class="label">Abstracción:</span> Te abstrae del sistema operativo y el hardware, pero sigues gestionando <em>instancias</em> o <em>aplicaciones</em> que están corriendo continuamente.</li>
                <li><span class="label">Escalado:</span> Escala bien, pero a menudo necesitas configurar reglas de autoescalado y puede que no escale a cero (sigues pagando por una instancia mínima).</li>
                <li><span class="label">Control:</span> Tienes más control sobre el entorno de ejecución.</li>
                <li><span class="label">Ejemplo:</span> Desplegar una aplicación completa en App Engine.</li>
              </ul>
            </div>
            
            <div class="comparison-item">
              <h4>Serverless (Computación sin servidor)</h4>
              <ul class="info-list">
                <li><span class="label">Abstracción:</span> Es el siguiente nivel. Te abstraes completamente de los servidores. Solo te preocupas por el código (funciones) o el contenedor.</li>
                <li><span class="label">Escalado:</span> Escala automáticamente desde cero hasta miles de peticiones, y vuelve a cero. Es ideal para cargas de trabajo impredecibles.</li>
                <li><span class="label">Control:</span> Menos control sobre el entorno, ya que es totalmente gestionado.</li>
                <li><span class="label">Modelo de pago:</span> <strong>Pago por ejecución</strong>. Si tu código no se ejecuta, no pagas nada.</li>
                <li><span class="label">Ejemplo:</span> Cloud Functions (código basado en eventos) o Cloud Run (contenedores).</li>
              </ul>
            </div>
          </div>
        </div>

        <div class="content-card">
          <h3 class="card-title">SaaS (Software como Servicio)</h3>
          <ul class="info-list">
            <li><span class="label">¿Qué es?</span> Proporciona una aplicación completa basada en la nube que los usuarios finales consumen directamente a través de internet.</li>
            <li><span class="label">Ejemplo:</span> Gmail, Google Docs, Google Drive.</li>
          </ul>
        </div>

        <!-- Quiz 1 -->
        <div class="quiz-container">
          <h3 class="quiz-title">Quiz: Modelos de Servicio</h3>
          <div class="quiz-question">
            <p class="question-text">1. ¿Cuál es la principal diferencia entre IaaS y PaaS en términos de modelo de pago?</p>
            <div class="quiz-options">
              <label class="quiz-option">
                <input type="radio" name="q1" value="a">
                <span>IaaS cobra por uso real, PaaS por recursos asignados</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q1" value="b">
                <span>IaaS cobra por recursos asignados por adelantado, PaaS por recursos realmente utilizados</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q1" value="c">
                <span>Ambos cobran de la misma manera</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q1" value="d">
                <span>PaaS siempre es más caro que IaaS</span>
              </label>
            </div>
            <div class="quiz-feedback"></div>
          </div>

          <div class="quiz-question">
            <p class="question-text">2. ¿Qué característica define a Serverless frente a PaaS?</p>
            <div class="quiz-options">
              <label class="quiz-option">
                <input type="radio" name="q2" value="a">
                <span>Serverless requiere más configuración manual</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q2" value="b">
                <span>Serverless escala automáticamente desde cero y solo pagas por ejecución</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q2" value="c">
                <span>PaaS es siempre más escalable que Serverless</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q2" value="d">
                <span>Serverless no puede escalar a cero</span>
              </label>
            </div>
            <div class="quiz-feedback"></div>
          </div>

          <button class="quiz-submit">Verificar Respuestas</button>
          <div class="quiz-result"></div>
        </div>
      </section>

      <!-- Section 2: Red de Google Cloud -->
      <section class="topic-section" id="red-gcp">
        <h2 class="section-title">2. Red de Google Cloud</h2>
        
        <div class="content-card">
          <p class="highlight-text">La red global de Google está diseñada para ofrecer el <strong>máximo rendimiento y la menor latencia posible</strong> utilizando más de 100 nodos de caché de contenido en todo el mundo.</p>
        </div>

        <div class="content-card">
          <h3 class="card-title">Regiones y Zonas</h3>
          <ul class="info-list">
            <li><span class="label">Región:</span> Un área geográfica independiente.</li>
            <li><span class="label">Zona:</span> Un área de despliegue de recursos <strong>dentro de una región</strong>. Las zonas están aisladas entre sí para proteger contra fallos.</li>
          </ul>
          
          <div class="example-box">
            <strong>Ejemplo:</strong> La región de Londres (<code>europe-west2</code>) se compone de tres zonas: <code>europe-west2-a</code>, <code>europe-west2-b</code> y <code>europe-west2-c</code>.
          </div>
          
          <p class="info-text">Desplegar recursos en múltiples zonas o regiones aumenta la disponibilidad y protege contra desastres.</p>
        </div>

        <div class="content-card">
          <h3 class="card-title">Recursos Multi-región</h3>
          <p class="info-text">Algunos servicios, como <strong>Spanner</strong>, permiten replicar datos no solo en múltiples zonas, sino en múltiples regiones, ofreciendo una resiliencia y disponibilidad globales.</p>
          <div class="image-placeholder">
            <span>📊 Imagen: Spanner Multi-región</span>
          </div>
        </div>

        <!-- Quiz 2 -->
        <div class="quiz-container">
          <h3 class="quiz-title">Quiz: Red de Google Cloud</h3>
          <div class="quiz-question">
            <p class="question-text">1. ¿Qué es una zona en Google Cloud?</p>
            <div class="quiz-options">
              <label class="quiz-option">
                <input type="radio" name="q3" value="a">
                <span>Un área geográfica independiente</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q3" value="b">
                <span>Un área de despliegue de recursos dentro de una región, aislada para proteger contra fallos</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q3" value="c">
                <span>Un datacenter específico</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q3" value="d">
                <span>Una red privada virtual</span>
              </label>
            </div>
            <div class="quiz-feedback"></div>
          </div>

          <button class="quiz-submit">Verificar Respuestas</button>
          <div class="quiz-result"></div>
        </div>
      </section>

      <!-- Section 3: Impacto Medioambiental -->
      <section class="topic-section" id="impacto">
        <h2 class="section-title">3. Impacto Medioambiental</h2>
        
        <div class="content-card">
          <p class="highlight-text">Los centros de datos consumen aproximadamente el <strong>2% de la electricidad mundial</strong>. Google se enfoca en la eficiencia energética y la sostenibilidad para minimizar este impacto.</p>
        </div>

        <!-- Quiz 3 -->
        <div class="quiz-container">
          <h3 class="quiz-title">Quiz: Sostenibilidad</h3>
          <div class="quiz-question">
            <p class="question-text">1. ¿Qué porcentaje de la electricidad mundial consumen los centros de datos?</p>
            <div class="quiz-options">
              <label class="quiz-option">
                <input type="radio" name="q4" value="a">
                <span>Aproximadamente 2%</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q4" value="b">
                <span>Aproximadamente 10%</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q4" value="c">
                <span>Aproximadamente 5%</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q4" value="d">
                <span>Menos del 1%</span>
              </label>
            </div>
            <div class="quiz-feedback"></div>
          </div>

          <button class="quiz-submit">Verificar Respuestas</button>
          <div class="quiz-result"></div>
        </div>
      </section>

      <!-- Section 4: Seguridad por Capas -->
      <section class="topic-section" id="seguridad">
        <h2 class="section-title">4. Seguridad por Capas</h2>
        
        <div class="content-card">
          <p class="intro-text">La infraestructura de seguridad de Google se organiza en capas progresivas:</p>
        </div>

        <div class="content-card">
          <h3 class="card-title">1. Capa de Infraestructura de Hardware</h3>
          <ul class="info-list">
            <li><span class="label">Diseño y Procedencia del Hardware:</span> Google diseña su propio hardware, incluyendo servidores, equipos de red y chips de seguridad personalizados (como el chip Titan).</li>
            <li><span class="label">Arranque Seguro (Secure Boot Stack):</span> Se utilizan firmas criptográficas en BIOS, bootloader, kernel y el sistema operativo base para asegurar que solo se ejecuta software verificado.</li>
            <li><span class="label">Seguridad Física de las Instalaciones:</span> Google diseña y construye sus propios centros de datos con múltiples capas de protección física.</li>
          </ul>
        </div>

        <div class="content-card">
          <h3 class="card-title">2. Capa de Despliegue de Servicios</h3>
          <ul class="info-list">
            <li><span class="label">Cifrado en Tránsito:</span> La comunicación entre servicios (llamadas RPC) se cifra automáticamente para garantizar la privacidad e integridad de los datos en la red, especialmente cuando viajan entre centros de datos.</li>
          </ul>
          <div class="image-placeholder">
            <span>🔒 Imagen: RPC Cifrado</span>
          </div>
        </div>

        <div class="content-card">
          <h3 class="card-title">3. Capa de Identidad de Usuario</h3>
          <ul class="info-list">
            <li><span class="label">Servicio Central de Identidad:</span> Gestiona la autenticación de los usuarios y empleados, aplicando medidas como el <strong>segundo factor de autenticación (2FA/U2F)</strong> y analizando el contexto del inicio de sesión (dispositivo, ubicación, etc.).</li>
          </ul>
        </div>

        <div class="content-card">
          <h3 class="card-title">4. Capa de Servicios de Almacenamiento</h3>
          <ul class="info-list">
            <li><span class="label">Cifrado en Reposo (Encryption at Rest):</span> Todos los datos almacenados en los servicios de Google se cifran por defecto. Se utiliza cifrado por hardware en los discos duros y SSDs para un rendimiento óptimo.</li>
          </ul>
        </div>

        <div class="content-card">
          <h3 class="card-title">5. Capa de Comunicación por Internet</h3>
          <ul class="info-list">
            <li><span class="label">Google Front End (GFE):</span> Es el servicio que gestiona todo el tráfico entrante desde internet.
              <ul class="sub-list">
                <li>Termina las conexiones <strong>TLS</strong> de forma segura.</li>
                <li>Utiliza certificados de autoridades certificadoras (CA).</li>
                <li>Implementa <strong>Perfect Forward Secrecy</strong>.</li>
                <li>Aplica protecciones contra ataques de <strong>Denegación de Servicio (DoS)</strong>.</li>
              </ul>
            </li>
          </ul>
        </div>

        <div class="content-card">
          <h3 class="card-title">6. Capa de Seguridad Operacional</h3>
          <ul class="info-list">
            <li><span class="label">Detección de Intrusiones:</span> Se utilizan reglas y machine learning para alertar a los equipos de seguridad sobre posibles incidentes.</li>
            <li><span class="label">Reducción del Riesgo Interno (Insider Risk):</span> Se limita y monitoriza activamente el acceso administrativo de los empleados a la infraestructura.</li>
            <li><span class="label">Uso de U2F para Empleados:</span> Para proteger contra ataques de phishing, las cuentas de los empleados requieren el uso de llaves de seguridad compatibles con U2F.</li>
            <li><span class="label">Prácticas de Desarrollo de Software Seguras:</span>
              <ul class="sub-list">
                <li>Revisión de código por dos personas.</li>
                <li>Librerías internas para prevenir vulnerabilidades comunes.</li>
                <li>Programa de recompensas por bugs (Bug Bounty).</li>
              </ul>
            </li>
          </ul>
        </div>

        <!-- Quiz 4 -->
        <div class="quiz-container">
          <h3 class="quiz-title">Quiz: Seguridad en Google Cloud</h3>
          <div class="quiz-question">
            <p class="question-text">1. ¿Qué componente gestiona todo el tráfico entrante desde internet en Google Cloud?</p>
            <div class="quiz-options">
              <label class="quiz-option">
                <input type="radio" name="q5" value="a">
                <span>Cloud Router</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q5" value="b">
                <span>Google Front End (GFE)</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q5" value="c">
                <span>Compute Engine</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q5" value="d">
                <span>Cloud VPN</span>
              </label>
            </div>
            <div class="quiz-feedback"></div>
          </div>

          <div class="quiz-question">
            <p class="question-text">2. ¿Qué tipo de cifrado utiliza Google para proteger los datos almacenados?</p>
            <div class="quiz-options">
              <label class="quiz-option">
                <input type="radio" name="q6" value="a">
                <span>Solo cifrado en tránsito</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q6" value="b">
                <span>Cifrado en reposo por defecto con hardware dedicado</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q6" value="c">
                <span>No utiliza cifrado</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q6" value="d">
                <span>Solo cifrado opcional</span>
              </label>
            </div>
            <div class="quiz-feedback"></div>
          </div>

          <button class="quiz-submit">Verificar Respuestas</button>
          <div class="quiz-result"></div>
        </div>
      </section>

      <!-- Section 5: Ecosistema Open Source -->
      <section class="topic-section" id="opensource">
        <h2 class="section-title">5. Ecosistema Open Source</h2>
        
        <div class="content-card">
          <p class="highlight-text">Para evitar el "vendor lock-in" (dependencia de un solo proveedor), Google Cloud se apoya fuertemente en el ecosistema de código abierto, permitiendo a los clientes migrar sus aplicaciones si lo necesitan.</p>
        </div>

        <div class="content-card">
          <h3 class="card-title">Proyectos Open Source de Google</h3>
          <ul class="info-list">
            <li><span class="label">TensorFlow:</span> Librería de software open source para Machine Learning desarrollada por Google.</li>
            <li><span class="label">Kubernetes (GKE):</span> Permite orquestar microservicios y distribuirlos entre diferentes nubes.</li>
            <li><span class="label">Google Cloud Observability:</span> Permite monitorizar cargas de trabajo que se ejecutan en múltiples proveedores de nube.</li>
          </ul>
        </div>

        <!-- Quiz 5 -->
        <div class="quiz-container">
          <h3 class="quiz-title">Quiz: Open Source</h3>
          <div class="quiz-question">
            <p class="question-text">1. ¿Qué ventaja principal ofrece el uso de tecnologías open source en Google Cloud?</p>
            <div class="quiz-options">
              <label class="quiz-option">
                <input type="radio" name="q7" value="a">
                <span>Evitar el vendor lock-in y permitir portabilidad</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q7" value="b">
                <span>Reducir costes siempre</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q7" value="c">
                <span>Mayor velocidad de procesamiento</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q7" value="d">
                <span>Menos seguridad pero más flexibilidad</span>
              </label>
            </div>
            <div class="quiz-feedback"></div>
          </div>

          <button class="quiz-submit">Verificar Respuestas</button>
          <div class="quiz-result"></div>
        </div>
      </section>

      <!-- Section 6: Precios y Facturación -->
      <section class="topic-section" id="precios">
        <h2 class="section-title">6. Precios y Facturación</h2>
        
        <div class="content-card">
          <h3 class="card-title">Modelos de Precios Clave</h3>
          <ul class="info-list">
            <li><span class="label">Facturación por segundo:</span> Google fue pionero en este modelo para sus servicios IaaS. Se aplica a:
              <ul class="sub-list">
                <li>Compute Engine (IaaS)</li>
                <li>Google Kubernetes Engine (GKE)</li>
                <li>Dataproc (Big Data)</li>
                <li>App Engine Flexible Environment (PaaS)</li>
              </ul>
            </li>
            <li><span class="label">Descuentos por uso continuo (Sustained-use discounts):</span> Descuentos automáticos que se aplican al ejecutar una VM durante más del 25% del mes. A más uso, mayor descuento.</li>
            <li><span class="label">Tipos de máquinas personalizadas (Custom VM types):</span> Permite ajustar con precisión la vCPU y la memoria de una VM para optimizar el coste según la necesidad de la aplicación.</li>
          </ul>
        </div>

        <div class="content-card">
          <h3 class="card-title">Herramientas de Control de Costes</h3>
          <ul class="info-list">
            <li><span class="label">Presupuestos (Budgets):</span>
              <ul class="sub-list">
                <li>Se pueden definir a nivel de cuenta de facturación o por proyecto.</li>
                <li>Pueden ser un límite fijo o un porcentaje del gasto del mes anterior.</li>
              </ul>
            </li>
            <li><span class="label">Alertas (Alerts):</span>
              <ul class="sub-list">
                <li>Notifican cuando el gasto se acerca al límite del presupuesto.</li>
                <li>Se pueden configurar en porcentajes (ej. 50%, 90%, 100%) o valores personalizados.</li>
              </ul>
            </li>
            <li><span class="label">Informes (Reports):</span> Herramienta visual en la consola para monitorizar gastos por proyecto o servicio.</li>
            <li><span class="label">Calculadora de precios online:</span> Para estimar costes.</li>
          </ul>
        </div>

        <div class="content-card">
          <h3 class="card-title">Cuotas (Quotas)</h3>
          <p class="intro-text">Las cuotas están diseñadas para prevenir el consumo excesivo de recursos (por error o ataque), protegiendo tanto al cliente como a la comunidad de Google Cloud. Se aplican a nivel de proyecto.</p>
          
          <ul class="info-list">
            <li><span class="label">Cuotas de Tasa (Rate Quotas):</span>
              <ul class="sub-list">
                <li>Limitan la cantidad de acciones en un período de tiempo.</li>
                <li><strong>Ejemplo:</strong> 3.000 llamadas a la API de GKE cada 100 segundos. Se resetea pasado el tiempo.</li>
              </ul>
            </li>
            <li><span class="label">Cuotas de Asignación (Allocation Quotas):</span>
              <ul class="sub-list">
                <li>Limitan el número total de recursos que puedes tener.</li>
                <li><strong>Ejemplo:</strong> Un máximo de 15 redes VPC por proyecto.</li>
              </ul>
            </li>
            <li><span class="label">Aumento de Cuotas:</span> Se puede solicitar un aumento a través del soporte de Google Cloud.</li>
          </ul>
        </div>

        <!-- Quiz 6 -->
        <div class="quiz-container">
          <h3 class="quiz-title">Quiz: Precios y Facturación</h3>
          <div class="quiz-question">
            <p class="question-text">1. ¿A partir de qué porcentaje de uso mensual se aplican los descuentos por uso continuo?</p>
            <div class="quiz-options">
              <label class="quiz-option">
                <input type="radio" name="q8" value="a">
                <span>10%</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q8" value="b">
                <span>25%</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q8" value="c">
                <span>50%</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q8" value="d">
                <span>75%</span>
              </label>
            </div>
            <div class="quiz-feedback"></div>
          </div>

          <div class="quiz-question">
            <p class="question-text">2. ¿Qué tipo de cuota limita el número total de recursos que puedes tener en un proyecto?</p>
            <div class="quiz-options">
              <label class="quiz-option">
                <input type="radio" name="q9" value="a">
                <span>Cuotas de Tasa (Rate Quotas)</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q9" value="b">
                <span>Cuotas de Asignación (Allocation Quotas)</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q9" value="c">
                <span>Cuotas de Presupuesto</span>
              </label>
              <label class="quiz-option">
                <input type="radio" name="q9" value="d">
                <span>Cuotas de Facturación</span>
              </label>
            </div>
            <div class="quiz-feedback"></div>
          </div>

          <button class="quiz-submit">Verificar Respuestas</button>
          <div class="quiz-result"></div>
        </div>
      </section>

    </div>
  </div>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    .gcp-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 32px 24px 80px;
      color: rgba(255, 255, 255, 0.92);
    }

    .navigation-back {
      margin-bottom: 24px;
    }

    .navigation-back a {
      color: #a5b4fc;
      text-decoration: none;
      font-size: 0.95rem;
      padding: 8px 16px;
      border-radius: 8px;
      background: rgba(122, 107, 255, 0.1);
      border: 1px solid rgba(122, 107, 255, 0.3);
      display: inline-block;
      transition: all 0.3s ease;
    }

    .navigation-back a:hover {
      background: rgba(122, 107, 255, 0.2);
      border-color: rgba(122, 107, 255, 0.5);
      transform: translateX(-4px);
    }

    .gcp-hero {
      text-align: center;
      padding: 48px 24px;
      margin-bottom: 48px;
      background: linear-gradient(135deg, rgba(122, 107, 255, 0.15) 0%, rgba(165, 180, 252, 0.08) 100%);
      border-radius: 20px;
      border: 1px solid rgba(122, 107, 255, 0.3);
    }

    .gcp-title {
      font-size: 2.5rem;
      font-weight: 700;
      color: #a5b4fc;
      margin-bottom: 12px;
      text-shadow: 0 2px 20px rgba(122, 107, 255, 0.3);
    }

    .gcp-subtitle {
      font-size: 1.15rem;
      color: rgba(255, 255, 255, 0.7);
      max-width: 700px;
      margin: 0 auto;
    }

    .content-section {
      display: flex;
      flex-direction: column;
      gap: 48px;
    }

    .topic-section {
      scroll-margin-top: 100px;
    }

    .section-title {
      font-size: 2rem;
      font-weight: 700;
      color: #c7d2fe;
      margin-bottom: 24px;
      padding-bottom: 12px;
      border-bottom: 2px solid rgba(122, 107, 255, 0.3);
    }

    .content-card {
      background: rgba(255, 255, 255, 0.02);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      padding: 24px;
      margin-bottom: 20px;
      transition: all 0.3s ease;
    }

    .content-card:hover {
      background: rgba(255, 255, 255, 0.04);
      border-color: rgba(122, 107, 255, 0.3);
    }

    .card-title {
      font-size: 1.4rem;
      font-weight: 600;
      color: #a5b4fc;
      margin-bottom: 16px;
    }

    .card-intro,
    .intro-text {
      font-size: 1.05rem;
      color: rgba(255, 255, 255, 0.8);
      margin-bottom: 16px;
      line-height: 1.6;
    }

    .info-list {
      list-style: none;
      padding-left: 0;
    }

    .info-list > li {
      padding: 12px 0;
      line-height: 1.7;
      color: rgba(255, 255, 255, 0.85);
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }

    .info-list > li:last-child {
      border-bottom: none;
    }

    .sub-list {
      list-style: disc;
      margin-left: 24px;
      margin-top: 8px;
    }

    .sub-list li {
      padding: 4px 0;
      color: rgba(255, 255, 255, 0.75);
    }

    .label {
      color: #a5b4fc;
      font-weight: 600;
      margin-right: 6px;
    }

    .highlight-text {
      font-size: 1.1rem;
      line-height: 1.7;
      color: rgba(255, 255, 255, 0.9);
      padding: 16px;
      background: rgba(122, 107, 255, 0.1);
      border-left: 4px solid #7a6bff;
      border-radius: 8px;
    }

    .info-text {
      line-height: 1.7;
      color: rgba(255, 255, 255, 0.85);
      margin-top: 12px;
    }

    .comparison-card {
      background: rgba(122, 107, 255, 0.05);
    }

    .comparison-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin-top: 16px;
    }

    .comparison-item {
      padding: 16px;
      background: rgba(255, 255, 255, 0.02);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 8px;
    }

    .comparison-item h4 {
      color: #c7d2fe;
      margin-bottom: 12px;
      font-size: 1.1rem;
    }

    .example-box {
      background: rgba(165, 180, 252, 0.1);
      border-left: 4px solid #a5b4fc;
      padding: 16px;
      margin: 16px 0;
      border-radius: 8px;
      line-height: 1.6;
    }

    .example-box code {
      background: rgba(0, 0, 0, 0.3);
      padding: 2px 8px;
      border-radius: 4px;
      font-family: 'Courier New', monospace;
      color: #fbbf24;
    }

    .image-placeholder {
      background: rgba(122, 107, 255, 0.1);
      border: 2px dashed rgba(122, 107, 255, 0.3);
      border-radius: 8px;
      padding: 32px;
      text-align: center;
      margin: 16px 0;
      color: rgba(255, 255, 255, 0.6);
      font-size: 1.1rem;
    }

    /* Quiz Styles */
    .quiz-container {
      background: rgba(122, 107, 255, 0.08);
      border: 1px solid rgba(122, 107, 255, 0.3);
      border-radius: 12px;
      padding: 28px;
      margin-top: 32px;
    }

    .quiz-title {
      font-size: 1.4rem;
      font-weight: 600;
      color: #c7d2fe;
      margin-bottom: 24px;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .quiz-title::before {
      content: "📝";
      font-size: 1.6rem;
    }

    .quiz-question {
      margin-bottom: 28px;
      padding: 20px;
      background: rgba(255, 255, 255, 0.02);
      border-radius: 8px;
    }

    .question-text {
      font-size: 1.05rem;
      font-weight: 500;
      color: rgba(255, 255, 255, 0.9);
      margin-bottom: 16px;
      line-height: 1.6;
    }

    .quiz-options {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .quiz-option {
      display: flex;
      align-items: center;
      padding: 14px 16px;
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .quiz-option:hover {
      background: rgba(122, 107, 255, 0.1);
      border-color: rgba(122, 107, 255, 0.4);
    }

    .quiz-option input[type="radio"] {
      margin-right: 12px;
      cursor: pointer;
      width: 18px;
      height: 18px;
    }

    .quiz-option span {
      color: rgba(255, 255, 255, 0.85);
      line-height: 1.5;
    }

    .quiz-option.correct {
      background: rgba(34, 197, 94, 0.15);
      border-color: rgba(34, 197, 94, 0.5);
    }

    .quiz-option.incorrect {
      background: rgba(239, 68, 68, 0.15);
      border-color: rgba(239, 68, 68, 0.5);
    }

    .quiz-feedback {
      margin-top: 12px;
      padding: 12px;
      border-radius: 6px;
      font-size: 0.95rem;
      line-height: 1.5;
      display: none;
    }

    .quiz-feedback.show {
      display: block;
    }

    .quiz-feedback.correct {
      background: rgba(34, 197, 94, 0.15);
      border: 1px solid rgba(34, 197, 94, 0.4);
      color: #86efac;
    }

    .quiz-feedback.incorrect {
      background: rgba(239, 68, 68, 0.15);
      border: 1px solid rgba(239, 68, 68, 0.4);
      color: #fca5a5;
    }

    .quiz-submit {
      background: linear-gradient(135deg, #7a6bff 0%, #a5b4fc 100%);
      color: white;
      border: none;
      padding: 14px 32px;
      border-radius: 8px;
      font-size: 1.05rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      margin-top: 8px;
    }

    .quiz-submit:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(122, 107, 255, 0.4);
    }

    .quiz-submit:active {
      transform: translateY(0);
    }

    .quiz-result {
      margin-top: 20px;
      padding: 16px;
      border-radius: 8px;
      font-size: 1.05rem;
      font-weight: 600;
      text-align: center;
      display: none;
    }

    .quiz-result.show {
      display: block;
    }

    .quiz-result.pass {
      background: rgba(34, 197, 94, 0.2);
      border: 2px solid rgba(34, 197, 94, 0.5);
      color: #86efac;
    }

    .quiz-result.fail {
      background: rgba(239, 68, 68, 0.2);
      border: 2px solid rgba(239, 68, 68, 0.5);
      color: #fca5a5;
    }

    @media (max-width: 768px) {
      .gcp-title {
        font-size: 1.8rem;
      }

      .section-title {
        font-size: 1.5rem;
      }

      .comparison-grid {
        grid-template-columns: 1fr;
      }

      .content-card {
        padding: 16px;
      }
    }
  </style>

  <script>
    // Quiz answers (correct answers)
    const quizAnswers = {
      q1: 'b', // IaaS cobra por recursos asignados, PaaS por uso real
      q2: 'b', // Serverless escala desde cero y pago por ejecución
      q3: 'b', // Zona es área dentro de región
      q4: 'a', // 2% electricidad mundial
      q5: 'b', // Google Front End (GFE)
      q6: 'b', // Cifrado en reposo por defecto
      q7: 'a', // Evitar vendor lock-in
      q8: 'b', // 25% uso mensual
      q9: 'b'  // Cuotas de Asignación
    };

    const quizExplanations = {
      q1: {
        correct: '¡Correcto! IaaS cobra por recursos asignados por adelantado, mientras que PaaS cobra por uso real.',
        incorrect: 'La respuesta correcta es: IaaS cobra por recursos asignados por adelantado, PaaS por recursos realmente utilizados.'
      },
      q2: {
        correct: '¡Exacto! Serverless escala automáticamente desde cero y solo pagas por ejecución real.',
        incorrect: 'La respuesta correcta es: Serverless escala automáticamente desde cero y solo pagas por ejecución.'
      },
      q3: {
        correct: '¡Perfecto! Una zona es un área de despliegue dentro de una región, aislada para protección contra fallos.',
        incorrect: 'La respuesta correcta es: Un área de despliegue de recursos dentro de una región, aislada para proteger contra fallos.'
      },
      q4: {
        correct: '¡Correcto! Los centros de datos consumen aproximadamente el 2% de la electricidad mundial.',
        incorrect: 'La respuesta correcta es: Aproximadamente 2% de la electricidad mundial.'
      },
      q5: {
        correct: '¡Exacto! Google Front End (GFE) gestiona todo el tráfico entrante desde internet.',
        incorrect: 'La respuesta correcta es: Google Front End (GFE).'
      },
      q6: {
        correct: '¡Perfecto! Google cifra todos los datos en reposo por defecto usando hardware dedicado.',
        incorrect: 'La respuesta correcta es: Cifrado en reposo por defecto con hardware dedicado.'
      },
      q7: {
        correct: '¡Correcto! El open source permite evitar el vendor lock-in y mantener portabilidad.',
        incorrect: 'La respuesta correcta es: Evitar el vendor lock-in y permitir portabilidad.'
      },
      q8: {
        correct: '¡Exacto! Los descuentos por uso continuo se aplican al usar VMs más del 25% del mes.',
        incorrect: 'La respuesta correcta es: 25% del mes.'
      },
      q9: {
        correct: '¡Perfecto! Las Cuotas de Asignación limitan el número total de recursos.',
        incorrect: 'La respuesta correcta es: Cuotas de Asignación (Allocation Quotas).'
      }
    };

    document.addEventListener('DOMContentLoaded', () => {
      const quizContainers = document.querySelectorAll('.quiz-container');
      
      quizContainers.forEach(container => {
        const submitBtn = container.querySelector('.quiz-submit');
        
        submitBtn.addEventListener('click', () => {
          const questions = container.querySelectorAll('.quiz-question');
          let correctCount = 0;
          let totalQuestions = questions.length;

          questions.forEach(question => {
            const options = question.querySelectorAll('input[type="radio"]');
            const feedback = question.querySelector('.quiz-feedback');
            let selectedOption = null;
            let questionName = null;

            options.forEach(option => {
              if (option.checked) {
                selectedOption = option;
                questionName = option.name;
              }
              // Reset styles
              option.parentElement.classList.remove('correct', 'incorrect');
            });

            if (selectedOption && questionName) {
              const selectedValue = selectedOption.value;
              const correctAnswer = quizAnswers[questionName];
              const isCorrect = selectedValue === correctAnswer;

              if (isCorrect) {
                correctCount++;
                selectedOption.parentElement.classList.add('correct');
                feedback.className = 'quiz-feedback correct show';
                feedback.textContent = quizExplanations[questionName].correct;
              } else {
                selectedOption.parentElement.classList.add('incorrect');
                feedback.className = 'quiz-feedback incorrect show';
                feedback.textContent = quizExplanations[questionName].incorrect;
                
                // Show correct answer
                options.forEach(opt => {
                  if (opt.value === correctAnswer) {
                    opt.parentElement.classList.add('correct');
                  }
                });
              }
            }
          });

          // Show result
          const resultDiv = container.querySelector('.quiz-result');
          const percentage = Math.round((correctCount / totalQuestions) * 100);
          
          if (percentage >= 70) {
            resultDiv.className = 'quiz-result pass show';
            resultDiv.textContent = `🎉 ¡Excelente! Has acertado ${correctCount} de ${totalQuestions} preguntas (${percentage}%)`;
          } else {
            resultDiv.className = 'quiz-result fail show';
            resultDiv.textContent = `📚 Has acertado ${correctCount} de ${totalQuestions} preguntas (${percentage}%). Revisa el contenido y vuelve a intentarlo.`;
          }

          // Disable submit button after checking
          submitBtn.disabled = true;
          submitBtn.style.opacity = '0.6';
          submitBtn.style.cursor = 'not-allowed';
        });
      });
    });
  </script>
</BaseLayout>
